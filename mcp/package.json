{
  "name": "@cmdpeek/mcp",
  "version": "0.2.0",
  "description": "MCP server for cmdpeek — discover recently installed CLI tools and gaps",
  "type": "module",
  "bin": {
    "cmdpeek-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "tsc && node --test dist/helpers.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
